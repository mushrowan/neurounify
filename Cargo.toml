[package]
name = "neurounify"
version = "0.1.0"
edition = "2024"
description = "universal eeg format converter"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
thiserror = "2"
